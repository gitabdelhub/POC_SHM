{
  "name": "saham-bank-analytics-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Saham Bank Analytics Portal — lancement local de l'API FastAPI + frontend vanilla",
  "scripts": {
    "dev": "concurrently -n API,FRONT -c blue,green \"npm run dev:api\" \"npm run dev:front\"",
    "dev:api": "cd backend && venv\\Scripts\\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "dev:front": "cd frontend && python -m http.server 5500"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
