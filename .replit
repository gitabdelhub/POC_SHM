language = "python3"
run = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8080"

[nix]
channel = "stable-23_11"

[deployment]
run = ["sh", "-c", "cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8080"]

[[ports]]
localPort = 8080
externalPort = 80
